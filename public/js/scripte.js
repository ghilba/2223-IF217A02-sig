
var splide = new Splide( '#new-product', {
    type: 'loop',
} );
splide.mount();

  var splide = new Splide( '#featured-product', {
    type   : 'loop',
    perPage: 3,
    perMove: 1,
  } );
  
  splide.mount();